pre-commit:
  parallel: true
  jobs:
    - name: Linting
      run: ./scripts/hooks.sh pnpm lint {staged_files} --fix
      stage_fixed: true
      only:
        staged: "*.{js,ts,tsx,jsx}"

    - name: Formatting
      run: ./scripts/hooks.sh pnpm format {staged_files}
      stage_fixed: true
